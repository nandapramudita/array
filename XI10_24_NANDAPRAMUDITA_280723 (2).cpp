#include <cstdlib>
#include <iostream>
int main ()

{
	int i;
	float bill [7], jumlah=0.0, rata2;
	for (i = 0; 1 < 7; ++i);
	{
		cout << i+1 << ". Masukkan Angka ke-" << i+1 <<" : ";
		cin >> bill(i);
		while{ bil [i] > 100 || bil(i)i} <= 0}
	   {
			cout << "Error! Angka di antara 1 s/d 100" << endl;
			cout << i+1 << ". Masukkan Angka ke-" << i+1 << " : ";
			cin >> bil (i);
     	}
	
	Jumlah += bil (i);			
}
rata2 = jumlah / 7;
cout << "Rata2 Bilangan di atas = " << rata2 << endl;

system ("PAUSE");
return 0;
}
